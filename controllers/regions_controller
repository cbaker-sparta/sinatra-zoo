class RegionsController < Sinatra::Base

  set :root, File.join(File.dirname(__FILE__), '..')

  set :views, Proc.new { File.join(root, 'views') }

  configure :development do
    register Sinatra::Reloader
  end

  get "/" do
    @title = "Regions"

    @regions = Region.all

    erb :'posts/regions/index'
  end

  # get "/regions/new" do
  #   @region = Region.new
  #
  #   erb :'posts/regions/new'
  # end
  #
  # post "/regions/" do
  #
  #   region = Region.new
  #
  #
  #   region.region = params[:region]
  #   region.map_image = params[:map_image]
  #   region.alt_tag = params[:alt_tag]
  #
  #
  #   region.save
  #
  #   redirect '/'
  # end
  #
  # get "/regions/:id" do
  #   regionId = params[:regionId].to_i
  #   @region = region.find(regionId)
  #   erb :'posts/regions/show'
  # end
  #
  # get "/regions/:id/edit" do
  #   regionId = params[:regionId].to_i
  #   @region = Region.find(regionId)
  #   erb :'posts/regions/edit'
  # end
  #
  # put "/regions/:id" do
  #   regionId = params[:regionId].to_i
  #   region = Region.find(regionId)
  #
  #
  #   region.region = params[:region]
  #   region.continent = params[:continent]
  #   region.map_image = params[:map_image]
  #   region.alt_tag = params[:alt_tag]
  #
  #   region.save
  #
  #   redirect '/regions/'
  # end
  #
  # delete "/:id" do
  #   regionId = params[:regionId].to_i
  #   region.destroy(regionId)
  #   redirect '/regions/'
  # end

end
